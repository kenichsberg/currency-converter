{
  "name": "currency-converter",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "common"
  ],
  "scripts": {
    "init:backend": "cd backend && npm ci",
    "init:frontend": "cd frontend && npm ci",
    "init:shared": "cd shared && npm ci",
    "init-all": "npm ci && run-p init:*",
    "dev:backend": "cd backend && npm run build && npm run start",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "run-p dev:*"
  },
  "volta": {
    "node": "22.14.0",
    "npm": "11.2.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
